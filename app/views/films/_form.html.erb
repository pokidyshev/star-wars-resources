<%= form_for(film) do |f| %>
  <% if film.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(film.errors.count, "error") %> prohibited this film from being saved:</h2>

      <ul>
      <% film.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :director %>
    <%= f.text_field :director %>
  </div>

  <div class="field">
    <%= f.label :episode_id %>
    <%= f.number_field :episode_id %>
  </div>

  <div class="field">
    <%= f.label :opening_crawl %>
    <%= f.text_area :opening_crawl %>
  </div>

  <div class="field">
    <%= f.label :producer %>
    <%= f.text_field :producer %>
  </div>

  <div class="field">
    <%= f.label :release_date %>
    <%= f.date_select :release_date %>
  </div>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
